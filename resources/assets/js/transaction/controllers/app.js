(function(){

    'use strict';

    angular
        .module('inventory.transaction')
        .controller('AppController', Controller);

    Controller.$inject = [
    ];

    function Controller(
    ){

        var vm = this;

        return vm;
    }

})();
