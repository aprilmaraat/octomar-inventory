(function(){

    'use strict';

    angular
        .module('inventory.management')
        .controller('AppController', Controller);

    Controller.$inject = [
    ];

    function Controller(
    ){

        var vm = this;

        return vm;
    }

})();
